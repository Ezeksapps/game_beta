Pitch = up/down (world z), Yaw = left/right (world x)

NOTE: UI library may be ImGui, but figure out how to control the styling so everything looks pixelated
(though it should automatically do this based on the specified width/height in renderer, font can be pixelated as well)

TODO: Switch to anisotropic filter for better-looking textures



SPRITE SHEET SYSTEM LOG
-=-=-=-=-=-=-=-=-=-=-=-=-

Renderer cannot render a frame if Scene is empty or unassigned. renderFrame() should return immediately if such a thing is attempted
a Scene is loaded with setScene, a scene can be switched out for another with setScene()
after creation, a Scene will always exist until ~Renderer() is called, because Renderer only exists in order to render a Scene

Upon setScene() being called, m_pScene should be assigned to a new unique_ptr assigned to the newly-constructed Scene
setScene will not clear the sprite texture array, but it will reset m_numSprites to zero, so anything loaded in there from the
previously-loaded Scene is treated as if it didn't exist and is overwritten as needed. This is done with registerSprite(), which
will deal with loading the new sprite object's texture data into memory at index m_numSprites, and increment the counter.

For situations where the sprite sheet being used for an entity is being changed, swapSprite()
takes the index of the old sprite you wish to replace and the new Sprite object and handles the swap

(Though if the new sprite is smaller, then the residual space in memory used up by the old sprite won't be overwritten. This could be an issue
if there is a situation where a smaller sprite sheet is loaded first, followed by a larger one, as this would overwrite the pages corresponding
to a neightbouring sprite. Also, the size of the texture array is not dynamic. Initially it was set to m_maxInstances, but that is no longer possible
with the implementation of sprite sheets as the number of entities no longer corresponds to the number of single sprite pages.
To counteract both these issues, I must first determine the maximum accepted size grid that a sprite sheet may use. Then, when loading
any sprite, regardless of size, into memory, assign the matching number of pages to the sprite, even if not all are used. The size of the array
is also now set to m_maxInstances * maxSizeWidth * maxSizeHeight. This way, Sprites never overwrite each other and there is enough space
to store all sprite sheets.
For now, the maximum number of pages (frames) a sprite sheet may hold is 10 * 10. This might be adjusted later)

a Sprite refers to a single sprite sheet
each frame has a duration in milliseconds, which is given by the frameDurations vector.
Each sprite sheet contains the frames to animate a specific action for the Entity. This is repeated for all
possible directions the Entity may be facing, which are north, north-east, east, south-east, south, south-west, west and north-west.
(This means every sprite sheet has a maximum of 8 rows)
Since every row is just a different version of the animation for a particular direction, the number of frames per row is also uniform.
frameDurations is therefore the same size as the number of frames in a row.

Entity holds an enum AnimEvent, which represents the events an entity can be in. The JSON for an Entity holds the frame durations and the AnimEvents for an entity
as well as the filepaths to the PNG sprite sheets for these events. This JSON is loaded in Entity's constructor and fills the sprite map.
The sprite map has the AnimEvent as the key and the Sprite object as the value. The private filed m_pActiveSprite defines the active animation for the Entity.

This is all implemented, which leaves these things to deal with:
---
Entity needs to store the direction it is facing. The correct row of the Sprite can then be used. -- DONE
The rows corresponding to every direction must be defined as a constant.                          -- DONE

The renderer must count the frame (col) in the row that is currently being shown and only keep it rendered for the specified time period
(This means frame rate now needs to be controlled and taken into account), then loop back to the beginning

NOTE: Because all sprites now occupy equal amounts of memory, index can be repurposed to just be the entity number the sprite belongs
to. To find the start index in the tex array, do index * m_maxSpriteDimensions

Render currently renders all sprites with the full texture in the tex array, needs to be adjusted to only one frame


Loading a scene will run the constructor, register the default sprite sheet and setup a callback to swap sprite sheets whenever
the AnimEvent is changed by Entity::doAnimEvent()

Each sprite has a steady_clock timer, which is started on creation. this will be used to determine when to switch frames
(based on sprite->frameDurations)

Sprite sheets are in rows and columns, where each in each row are the frames for a particular direction
The row to access can be determined based on the direction of the entity, since each Direction enum val
is assigned a number equal to the row of the sprite sheet the anim for that direction will be

dimensions are at a max of 8 * 10 frames, there are currently 8 possible directions



Start index of the animation is therefore the start index of the Sprite in tex array (sprite->index * m_maxSpriteDimensions)
plus (uint8_t)Direction * (sprite width), end index is start index plus sprite width

CHECK: Should sprite width be stored in Sprite? it can be calculated at runtime using textureLoader->GetTextureDesc().GetWidth() / 192
(all sprites are 192 x 192 per frame), also, should the texture width and height be defined as constants?


SHADER MODIFICATION FOR SPRITE SHEET

VERTEX LAYOUT
-=-=-=-=-=-=-=-
vec3 pos                       [buf slot 0]
vec2 uv                        [buf slot 0]
mat4 transform (model matrix)  [buf slot 1]
int  texArrayIndex             [buf slot 1]

UV turns out to be entirely useless, since UVs are set in the geometry shader when growing the primitive anyway

The current vertex layout of the Sprite pipeline has a slot for the texture array index (copied from Diligent samples)
This is currently unused, but can be set to the index in the texture array of the frame to be drawn, which can be retrieved by:

const std::shared_ptr<Sprite> activeSprite = entity->getActiveSprite();
int frameArrayIndex = (activeSprite->index * m_maxSpriteDimensions) + ((uint8_t)entity->getDirection() * activeSprite->frame)

where activeSprite->frame is the zero-indexed frame number of one animation in one direction

Each slice of the texture array is defined to be a 192 * 192 box
The correct Diligent::Box to use as the texture for a given Entity can therefore be passed in texArrayIndex and used to assign the correct texture

The fragment shader is currently setting the `out vec4 color` to be: color = texture(g_texture, vec3(uv, 0.0));
The vec3 in that texture function works exactly the same as a Diligent::Box object when updating a texture, where the z coordinate
defines the number of slices in the texture (aka. frames). Because previously, only one 192 * 192 sprite existed in every texture, this could be left as 0.0f,
but I now need to access a specific frame/slice. The z-coordinate therefore should now be texArrayIndex

Instance buffer doesn't currently do anyting other than manage and update the model matrices,
the size of the buffer is set on creation to sizeof(mat4) * m_maxInstances, which is only enough to hold all the model matrices and nothing else
this should be updated to sizeof(mat4) * sizeof(int) * m_maxInstances, so that the texArrayIndex is included

model matrices are updated with:

uint32_t dataSize = static_cast<uint32_t>(sizeof(mat4) * m_instanceData.size());
m_pImmediateContext->UpdateBuffer(m_pSpriteInstanceBuffer, 0, dataSize, m_instanceData.data(), Diligent::RESOURCE_STATE_TRANSITION_MODE_TRANSITION);

IDeviceContext::UpdateBuffer should also be called to update the texArrayIndex, the question now becomes how?
Also, sprites/entities still are not using the clock to time their frames, the timing mechanism needs to be determined.

Currently done is:
- shaders now only draw one frame
- entity, scene and sprite classes/structs are all made
- setScene() properly loads all necessary sprites into memory and sets the sprite's index field
- entities have a sprite change callback linked to renderer so that sprite sheets are properly swapped out when the active sprite is changed
- timing frames and updating current Sprite frame using Entity::update() and Renderer's delta time

animation time can be updated every frame draw based on delta time between last frame and current frame
https://vikramsinghmtl.github.io/420-5P6-Game-Programming/concepts/delta-time/

all movement is also scaled by delta time with a system like this. This will be important when calling moveEntity along w/ an animation
(The entity should in that case be transformed by the specified vector, but movement is gradual so that the movement 'steps' are evenly
split up. The entity should stop at its final destination after one loop of the appropriate animation has completed.
In the case that the transform vector is for more than one position, then the animation loops x amount of times)


Vertex buffer redundant (pos not needed), remove

Instance buffer population does need to happen per frame, since Sprite animation frames are always changing
Run after calling update() for all entities to make sure frame index is up-to-date

TODO: Upscale PMD sprites for 192 * 192 frame dimensions
TODO: Fix shader bug

Movement API:

Entity::move() moves entity by one square (x/y grid) in any of the enumerated directions
TODO: Z-axis updates must be checked following translation by a collision check

Movement cannot exist w/o animation. Move must define mode of movement in a second param, this being an AnimEvent

Movement must then occur in a way so that the translation does not complete until the anim completes and both finish in sync

total anim time in ms can be found by summing all elems in sprite->frameDurations
entity::translate() must then be run in steps, translation in all axes being divided by the frame durations


World is a 2.5D system, the map is a glTF file loaded through the custom Renderer. This is handled specifically by the 'map' pipeline (might be renamed to Scene later)

Any map/scene is therefore a fully 3D environment. The projection matrix is a perspective projection. The camera is a custom quaternion based implementation that treats Z as its forward direction (so by default it overlooks the scene looking down -z)

The Engine is initialised by the platform entry point, which then hands the pointer off to game.cpp aka. The main game code.

This is currently on Linux (w/ X11 or Wayland) and Windows so the platform entry point uses GLFW

Engine holds two private members, the Scene and the Renderer, both unique pointers

Scene holds a vector of shared pointers to all Entity objects that the Renderer is rendering. This is done on the separate sprite pipeline,
which grows a billboard around the world-space position of the Entity and assigns the correct slice/frame of the sprite sheet texture onto it.

The objective now is to define the relations between all Engine's classes and the API interface

Entities have now been reworked so that all sprites are animated via a sprite sheet system.
Through a callback, the renderer will be notified if the sprite sheet changes and updates accordingly

Movement does not currently exist however. The basic concept is not difficult. All you have to do is update Entity position (a vec3)
Despite the world space being 3D, the way the game is played is still 2D. X and Y coordinates are the only ones that matter for game interactions.
The Z axis exists to provide 3D depth.

Movement is therefore done square-by-square (like in PMD)
The move function takes the Direction (enum) of movement and the mode (AnimEvent) of movement, which plays the correct animation. Each square is 1.0f * 1.0f
Origin is 0, 0

When position is updated I now most likely need another callback connected to renderer to
1. Check if the translation in any direction is > 1.0f, if so reject it
2. Split the transform so that it is done in sprite->frameDurations.size() steps (aka. numFrames), this way the animation plays smoothly across movement
3. Run transforms

Another issue is how to detect constant hold on a key w/ GLFW instead of single presses.
All other movement inputs in command queue should be ignored until Renderer completes current one.

NOTE: MaxZ truns out to not specify slices to update, a 2D texture must have them all updated one-by-one

Diligent Engine: ERROR in ValidateTextureRegion() (TextureBase.cpp, 386): Texture '0x2CCF9580': Region max Z (80) must be 1 for all but 3D textures.
Diligent Engine: ERROR: Debug assertion failed in ValidateUpdateTextureParams(), file TextureBase.cpp, line 441:
Source data depth stride (0) is below the image plane size (215040).

280 × 384, 40 x 48 per frame

7 rows, 8 cols

192 * 7 = 1344
192 * 8 = 1536

1344 * 1536


CHECK: Why is camera randomising its position now?
CHECK: Update instance buffer calcs for index clearly work, but checking frag shader shows texArrayIndex always == 0?

Texture array index: 42
Texture array index: 43
Texture array index: 44
Texture array index: 45
Texture array index: 46
Texture array index: 47
Texture array index: 48
Texture array index: 42
Texture array index: 43
Texture array index: 44
Texture array index: 45
Texture array index: 46
Texture array index: 47
Texture array index: 48

Print of result of (activeSprite->index * m_maxSpriteDimensions) + ((uint8_t)entity->m_direction * activeSprite->framesPerRow) + activeSprite->frame;
Aka. texArrayIndex, clearly correct, running every frame

Bug is vertex shader is somehow not recieving this data

[Low-pri bug]

Diligent Engine: Info: Desired back buffer count (2) is smaller than the minimal image count supported for this surface (3). Resetting to 3
Diligent Engine: Info: Created swap chain with 3 images vs 2 requested.


