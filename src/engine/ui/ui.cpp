/* UI related functions and core implementation */

#define NK_IMPLEMENTATION
#include "nuklear.h"

struct nk_user_font font;
struct nk_context ctx;

/*
font.userdata = nk_handle_ptr();
font.height = ;
font.width = ;
nk_init_fixed(&ctx, calloc(1, MAX_MEMORY), MAX_MEMORY, &font);*/
